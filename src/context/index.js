import {
  ProjectsContext,
  ProjectsProvider,
  useProjectsValue
} from './projects-context';

import {
  SelectedProjectContext,
  SelectedProjectProvider,
  useSelectedProjectValue
} from './selected-project-context';


// export it to the top level
export {
  ProjectsContext,
  ProjectsProvider,
  useProjectsValue,
  SelectedProjectContext,
  SelectedProjectProvider,
  useSelectedProjectValue
};


